import 'package:flutter/material.dart';
class UserPlantList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("UserPlantList page"),
    );
  }
}