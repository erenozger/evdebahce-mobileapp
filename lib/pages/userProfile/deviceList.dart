import 'package:flutter/material.dart';

class UserDeviceList extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("UserDeviceList page"),
    );
  }
}
